/// ドメイン: 振り返り履歴グループID
class DomainReflectionHistoryGroupId {
  const DomainReflectionHistoryGroupId({required this.id});
  final int id;
}

/// ドメイン: 振り返り履歴グループ日付
class DomainReflectionHistoryGroupDate {
  const DomainReflectionHistoryGroupDate({required this.date});
  final DateTime date;
}
